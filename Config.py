#Change this as needed, ReadDir is where you find the files, WriteDir is where you leave the resulting files.
Directories = {
    "SteamDir" : r"C:\Program Files (x86)\Steam\steamapps\workshop\content",
    "WriteDir" : r"",
}

#Change this as needed, pre-set to starbound but can be used for other games that use a similar system for mods.
CoreIDs = {
    "Game" : "211820"
}

#Change this as needed, pre-set to starbound but can be used for other games that use a similar system for mods.
ModInfo = {
    "ExpectedName" : "contents",
    "ExpectedExt" : ".pak"
}